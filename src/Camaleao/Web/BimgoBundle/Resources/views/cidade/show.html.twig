{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cidade</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cidade.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ cidade.nome }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cidade_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cidade_edit', { 'id': cidade.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
