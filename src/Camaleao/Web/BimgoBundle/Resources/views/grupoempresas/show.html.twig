{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupoempresas</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupoempresa.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ grupoempresa.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ grupoempresa.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('grupoempresas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('grupoempresas_edit', { 'id': grupoempresa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
