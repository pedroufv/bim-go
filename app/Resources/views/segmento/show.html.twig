{% extends 'base.html.twig' %}

{% block body %}
    <h1>Segmento</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ segmento.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ segmento.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ segmento.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('segmento_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('segmento_edit', { 'id': segmento.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
