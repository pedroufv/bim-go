{% extends 'base.html.twig' %}

{% block body %}
    <h1>Papel</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ papel.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ papel.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ papel.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('papel_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('papel_edit', { 'id': papel.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
